$!test-init
  test-file=test1.t
  title='Integer manipulations' [

  $!test-doc [
    Test the integer declaration and then assignment of a number. Test
    if the number
  ]

  $!test-code [
    my Int $i;
    is $i, Any, 'Uninitialized Int';
    $i = 10;
    is $i, 10, 'Initialized Int';
  ]
]
