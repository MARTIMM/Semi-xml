branches:
  only:
    - master

language: perl6

perl6:
  - latest

install:
  - rakudobrew build-panda ${TRAVIS_PERL6_VERSION#latest}
#  - panda installdeps .
  - panda update
  - panda --notests install XML
  - panda --notests install Config::DataLang::Refine
  - panda --notests install Terminal::ANSIColor

cache:
  directories:
    - $TRAVIS_BUILD_DIR/.rakudobrew
